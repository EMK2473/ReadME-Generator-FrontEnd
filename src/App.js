import React from "react";
import Form from "./Form"; // Update the path based on your file structure
import "./App.css"; // You can keep the default styles or modify them as needed

function App() {
  return (
    <div className="App">
      <Form />
    </div>
  );
}

export default App;
